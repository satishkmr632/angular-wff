export class LatestNews {
    public heading: string;
    public text: string;
    public category: string;

    constructor(heading: string, text: string, category: string) {
      this.heading = this.heading;
      this.text = text;
      this.category = category;
    }
  }